// Everything is client-side: the tracker's data lives on the machine running the game, and
// the drag-and-drop mode parses files in the browser. Nothing to render on a server.
export const ssr = false;
export const prerender = false;
